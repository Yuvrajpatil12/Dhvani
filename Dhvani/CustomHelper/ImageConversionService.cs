namespace Dhvani.CustomHelper
{
    public class ImageConversionService
    {
        //public byte[] ConvertToWebpOrJpg(byte[] imageData, string extension)
        //{
        //    using (var image = Image.Load(imageData))
        //    {
        //        if (extension.ToLower() == ".jpg" || extension.ToLower() == ".jpeg")
        //        {
        //            // Convert to JPG
        //            return image.SaveAsJpeg().ToArray();
        //        }
        //        else if (extension.ToLower() == ".webp")
        //        {
        //            // Convert to WebP
        //            return image.SaveAsWebP().ToArray();
        //        }
        //        else
        //        {
        //            throw new ArgumentException("Unsupported image format");
        //        }
        //    }
        //}
    }
}
